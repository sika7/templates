{
  "name": "wp_seed",
  "version": "1.0.0",
  "description": "webを素早く制作するための種",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css": "npx parcel watch 'theme/css/**/!(_)*.styl' -d theme/asset/",
    "js": "npx parcel watch 'theme/js/**/!(_)*.jsx' -d theme/asset/",
    "watch": "npx browser-sync start --proxy localhost:9000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.4",
    "parcel-bundler": "^1.8.1",
    "ress": "^1.2.2",
    "stylus": "^0.54.5"
  },
  "dependencies": {
    "@frctl/fractal": "^1.1.7",
    "jquery": "^3.3.1",
    "react": "^16.3.2"
  }
}
